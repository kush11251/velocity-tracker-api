# Velocity Tracker API
A high-performance API for tracking user activities and metrics.

## Installation
1. Run `go mod init velocity-tracker-api` to initialize the Go module.
2. Run `go get` to install dependencies.

## Execution
Run `go run src/main.go` to start the API server.
